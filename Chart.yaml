apiVersion: v2
name: platform-chart
description: >
  Chart Helm compartilhado para apps pessoais (API HTTP + frontend estático,
  com CronJob, Postgres e Redis self-hosted opcionais, e NetworkPolicy pra
  isolar os dois). Cada app declara isto como dependência no seu próprio
  Chart.yaml e só fornece um values.yaml.
type: application
version: 0.2.2
appVersion: "0.2.2"
keywords:
  - platform-chart
  - api
  - frontend
  - cronjob
maintainers:
  - name: Felipe
