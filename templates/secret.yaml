{{- if .Values.secrets.create }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "platform-chart.secretName" . }}
  labels:
    {{- include "platform-chart.labels" . | nindent 4 }}
type: Opaque
{{- if .Values.secrets.data }}
stringData:
  {{- range $key, $value := .Values.secrets.data }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
{{- else }}
stringData: {}
{{- end }}
{{- end }}
